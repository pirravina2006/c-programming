#include<stdio.h>
int main(){
    // int a,b,c;
    // scanf("%d %d %d",&a,&b,&c);
    // int tot=a+b+c;
    // printf("Total bill :%d",tot);
    // return 0;
    // int bill;
    // scanf("%d",&bill);

    // int share=bill/3;
    // printf("Each person share is :%d",share);
    // return 0;

    // int sal;
    // scanf("%d",&sal);
    // int res=sal/30;
    // printf("Daily salary of employee : %d",res);
    // return 0;

    // int math,phy,chem,tam,eng;
    // scanf("%d %d %d %d %d",&math,&phy,&chem,&tam,&eng);
    // int tot=math+phy+chem+tam+eng;
    // int avg=tot/5;
    // printf("Total marks: %d\n",tot);
    // printf("Average marks: %d",avg);
    // return 0;

    int len,bre;
    scanf("%d %d",&len,&bre);
    int area=len*bre;
    printf("Area of rectangle is :%d",area);
    return 0;
}